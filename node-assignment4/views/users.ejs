<%- include('includes/head.ejs') %>

<h1> Users </h1>

<% if (users.length > 0) { %>
    
    <ul>
        <% for (let u of users) { %>
            
            <li> <%= u.name %> </li>
            <% } %>


    </ul>

    <% } else {  %> 
        
        <h1> no users found </h1>
        <% } %> 
<%- include('includes/end.ejs')%>